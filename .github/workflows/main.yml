name: Deploy Lambda Function

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Deploy Lambda function
        run: |
          sam deploy \
            --template-file template.yml \
            --stack-name sam-app \
            --capabilities CAPABILITY_IAM \
            --no-confirm-changeset \
            --parameter-overrides ParameterKey=MONGODB_USERNAME,ParameterValue=yasitharanga1 ParameterKey=MONGODB_PASSWORD,ParameterValue=YRKTV642yrktv

        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

        # run: |
        #   aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
        #   aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        #   sam deploy --stack-name sam-app --region ap-south-1 --no-confirm-changeset --parameter-overrides "ParameterKey=MONGODB_USERNAME,ParameterValue=${{ secrets.MONGODB_USERNAME }}" "ParameterKey=MONGODB_PASSWORD,ParameterValue=${{ secrets.MONGODB_PASSWORD }}"
